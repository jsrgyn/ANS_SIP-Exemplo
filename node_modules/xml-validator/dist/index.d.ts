import { XmlTypes, XmlValidationResult, XmlValidatorOptions } from "./types";
export declare const defaultOptions: XmlValidatorOptions;
export declare class XmlValidator {
    private options;
    constructor(options?: XmlValidatorOptions);
    validate(content: string, type?: XmlTypes): XmlValidationResult;
    validateFile(url: string, type?: XmlTypes): Promise<XmlValidationResult>;
    validateUrl(url: string): Promise<XmlValidationResult>;
}
